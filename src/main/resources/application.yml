spring:
  cloud:
      mvc:
        token-relay:
          enabled: false
  datasource:
    url: jdbc:h2:mem:meteo;mode=oracle
    username: tiila
    password: tiila
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
  h2:
      console.enabled: true

  secrets:
    openweather:
      token: 5ae8e5ba9d5fd2580dee47b602bd7700
